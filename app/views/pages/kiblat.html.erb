<% content_for :title, "Arah Kiblat" %>
<% content_for :description, "Cari arah kiblat menggunakan kompas digital" %>

<div class="container mx-auto px-4 py-8 md:py-12 max-w-lg">
  <div class="backdrop-blur-md bg-base-100/30 rounded-3xl p-8 border border-white/20 shadow-2xl"
       data-controller="qibla"
       data-qibla-zone-lat-value="<%= @location['latitude'] %>"
       data-qibla-zone-lng-value="<%= @location['longitude'] %>"
       data-qibla-zone-name-value="<%= @location['location'] %>">

    <!-- Header -->
    <h1 class="text-2xl font-bold text-center mb-2">Arah Kiblat</h1>
    <p class="text-center text-base-content/60 text-sm mb-6" data-qibla-target="status">Memulakan...</p>

    <!-- Compass Container -->
    <div class="relative w-72 h-72 mx-auto mb-6">
      <!-- Outer ring for alignment highlight -->
      <div class="absolute inset-0 rounded-full transition-all duration-300 ease-out"
           data-qibla-target="ring"></div>

      <!-- Compass Rose (outer dial) - rotates with device -->
      <div class="absolute inset-0 rounded-full border-4 border-white/30 bg-base-100/20 transition-transform duration-100 ease-out"
           data-qibla-target="compass">

        <!-- Cardinal directions -->
        <div class="absolute top-2 left-1/2 -translate-x-1/2 text-lg font-bold text-primary">U</div>
        <div class="absolute bottom-2 left-1/2 -translate-x-1/2 text-lg font-bold text-base-content/60">S</div>
        <div class="absolute left-2 top-1/2 -translate-y-1/2 text-lg font-bold text-base-content/60">B</div>
        <div class="absolute right-2 top-1/2 -translate-y-1/2 text-lg font-bold text-base-content/60">T</div>

        <!-- Degree markers -->
        <% [30, 60, 120, 150, 210, 240, 300, 330].each do |deg| %>
          <div class="absolute w-1 h-3 bg-white/30 left-1/2 -translate-x-1/2"
               style="top: 4px; transform-origin: center <%= (272/2) - 4 %>px; transform: translateX(-50%) rotate(<%= deg %>deg);">
          </div>
        <% end %>
      </div>

      <!-- Qibla Direction Needle - points to Qibla -->
      <div class="absolute inset-0 flex items-center justify-center transition-transform duration-100 ease-out"
           data-qibla-target="needle"
           style="transform: rotate(0deg)">
        <!-- Needle body -->
        <div class="absolute w-3 h-28 bottom-1/2 left-1/2 -translate-x-1/2 origin-bottom">
          <div class="w-full h-full bg-gradient-to-t from-primary via-primary to-primary/70 rounded-full shadow-lg"></div>
          <!-- Kaaba indicator at top -->
          <div class="absolute -top-1 left-1/2 -translate-x-1/2 w-6 h-6 bg-primary rounded-sm rotate-45 flex items-center justify-center shadow-lg">
            <svg class="w-4 h-4 -rotate-45 fill-primary-content" viewBox="0 0 24 24">
              <path d="M12 2L2 7v10l10 5 10-5V7L12 2zm0 2.5L19 8v7l-7 3.5L5 15V8l7-3.5z"/>
            </svg>
          </div>
        </div>
        <!-- Opposite end (south indicator) -->
        <div class="absolute w-2 h-20 top-1/2 left-1/2 -translate-x-1/2 origin-top">
          <div class="w-full h-full bg-base-content/30 rounded-full"></div>
        </div>
      </div>

      <!-- Center point -->
      <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
        <div class="w-6 h-6 rounded-full bg-base-100 border-4 border-primary shadow-lg"></div>
      </div>
    </div>

    <!-- Bearing Display -->
    <div class="text-center mb-6">
      <div class="text-5xl font-bold tabular-nums" data-qibla-target="bearing">--°</div>
      <div class="text-sm text-base-content/60 mt-1">darjah dari Utara</div>
    </div>

    <!-- Location Info -->
    <div class="backdrop-blur-sm bg-white/10 rounded-2xl p-4 border border-white/10">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full bg-primary/20 flex items-center justify-center">
          <%= icon "map-pin", class: "size-5 text-primary" %>
        </div>
        <div class="flex-1">
          <div class="font-medium" data-qibla-target="location">Mengesan lokasi...</div>
          <div class="text-xs text-base-content/50" data-qibla-target="coordinates">-</div>
        </div>
      </div>
    </div>

    <!-- Permission Button (iOS) -->
    <button class="btn btn-primary btn-block mt-4 hidden"
            data-qibla-target="permissionBtn"
            data-action="click->qibla#requestPermission">
      <%= icon "compass", class: "size-5" %>
      Aktifkan Kompas
    </button>

    <!-- Warning Messages -->
    <div class="alert alert-warning mt-4 hidden" data-qibla-target="warning">
      <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
      </svg>
      <span></span>
    </div>

    <!-- Info -->
    <div class="mt-6 text-center text-xs text-base-content/50">
      <p>Halakan peranti anda mengikut arah jarum kompas untuk menghadap kiblat.</p>
      <p class="mt-1">Koordinat Kaabah: 21.4225°N, 39.8262°E</p>
    </div>
  </div>
</div>
